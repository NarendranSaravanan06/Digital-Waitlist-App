<Window x:Class="WaitListApp.Views.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Login"
        Height="450"
        Width="800"
        Background="#1e1e2f"
        Foreground="White"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI">

    <Grid>
        <Border Width="400" Height="300" Background="#2d2d40" CornerRadius="10" Padding="30" VerticalAlignment="Center" HorizontalAlignment="Center" >
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Stretch">
                <TextBlock Text="Login" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10" Foreground="DarkSeaGreen"/>

                <StackPanel>
                    <TextBlock Text="Username" FontSize="14" Margin="0,0,0,5"/>
                    <TextBox x:Name="userName"
                             Height="30"
                             FontSize="14"
                             Background="#fff"
                             Foreground="#000"
                             Padding="5"
                             BorderThickness="0"
                             TextChanged="userName_TextChanged"/>
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="Password" FontSize="14" Margin="0,10,0,5"/>
                    <PasswordBox x:Name="password"
                                 Height="30"
                                 FontSize="14"
                                 Background="#fff"
                                 Foreground="#000"
                                 Padding="5"
                                 BorderThickness="0"
                                 PasswordChanged="password_PasswordChanged"/>
                </StackPanel>

                <Button Content="Login"
                        Height="40"
                        FontSize="16"
                        Background="#007ACC"
                        Foreground="White"
                        BorderThickness="0"
                        Click="Button_Click"
                        Margin="0,20,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
